@model logisticTracker.Entities.Trip
@{
	ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<form id="editForm" action="/Trips/Edit" method="post">

	<input type="hidden" name="ID" value="@Model.ID" />
	<div class="form-group">
		<label>Vehicle No</label>
		<input class="form-control" name="VehicleNo" value="@Model.VehicleNo" />
	</div>
	<div class="form-group">
		<label>LoadingDate</label>
		<input class="form-control" name="LoadingDate" value="@Model.loadingDate" />
	</div>
	<div class="form-group">
		<label>KarachiRent</label>
		<input class="form-control" name="KarachiRent" value="@Model.KarachiRent" />
	</div>
	<div class="form-group">
		<label>Weight Details</label>
		<input class="form-control" name="Details" value="@Model.Details" />
	</div>
	<div class="form-group">
		<label>Weight in Tons</label>
		<input class="form-control" name="Weight" value="@Model.Weight" />
	</div>
	<button id="saveButton" type="button" class="btn btn-success">Save</button>
</form>

<script>

	$("#saveButton").click(function () {

			$.ajax({
				url: "@Url.Action("Edit", "Trips")",
				type: "post",
				data: $("#editForm").serialize()
			}).done(function (response) {

				$("#listingArea").html(response);
				window.location.reload();
			});
	});

</script>
